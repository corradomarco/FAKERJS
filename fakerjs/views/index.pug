doctype html
html
    head
        link(rel='stylesheet',href='/css/style.css')
        meta(name="viewport"content="width=device-width)
    body.style
      each poet in poeti
        device  
            a(href=`/users/profile?id=${poet.id}`)
            h1#{poet.nome}
            


